<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all' %>
    <%= stylesheet_pack_tag 'tailwindcss', media: 'all' %>
  </head>

  <body>
    <header style="margin-top:30px;">
      <nav>
        <% if user_signed_in? %> 
          Logged in as <strong><%= current_user.email %></strong>.
          <%= link_to 'プロフィール変更', edit_user_registration_path %> |
          <%= link_to "ログアウト", destroy_user_session_path %>
        <% else %>
          <%= link_to "サインアップ", new_user_registration_path, class: "nav-button" %> |
          <%= link_to "ログイン", new_user_session_path, class: "nav-button" %>
        <% end %>
      </nav>
    </header>

    <p class="notice bg-green-300 pl-4"><%= notice %></p>
    <p class="alert bg-red-200 pl-4"><%= alert %></p>

    <%= yield %>
  </body>
</html>
